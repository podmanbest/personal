# Dokumentasi Proyek

- **RUN-API.md** — **Cara menjalankan API**: konfigurasi, database, perintah `make run` / `go run`, build.
- **STRUKTUR-PROYEK.md** — Struktur proyek: penempatan file berdasarkan fungsinya (API, Web, root, CI).
- **SETUP-COMMANDS.md** — Langkah-langkah command line: inisialisasi proyek, tema minimalis (Pico CSS), struktur folder, dan deploy Podman (Alpine).
- **DEPLOY.md** — CI/CD (GitHub Actions), deploy GitHub Pages, auto-merge.
- **api/** — Dokumentasi API: [api/README.md](api/README.md) (endpoint, auth, config), [api/DATABASE.md](api/DATABASE.md), [api/DATABASE-SETUP.md](api/DATABASE-SETUP.md).
- **web/** — Dokumentasi frontend (routing, komponen, tema).

Referensi rencana dan fase: [ToDo.md](../ToDo.md).
