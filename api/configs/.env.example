# Salin ke .env dan isi nilai. Jangan commit .env.
# Password: minimal 12–16 karakter, huruf besar/kecil, angka, simbol. Detail: configs/README.md

PORT=8080

# Opsi 1: satu string DSN (prioritas jika di-set)
# DB_DSN=user:password@tcp(localhost:3306)/dbname?parseTime=true

# Opsi 2: pisah per komponen (jika DB_DSN kosong, DSN dibangun dari sini)
DB_USER=personal
DB_PASSWORD=your_secure_password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=personal

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
JWT_SECRET=minimal_32_character_secret_for_jwt_signing

# CORS: origin frontend (wajib jika frontend beda domain dari API). Jangan gunakan * di production.
# Development: ALLOW_ORIGIN=http://localhost:5173
# Production: ALLOW_ORIGIN=https://namaanda.com (atau https://username.github.io jika tanpa custom domain)
# ALLOW_ORIGIN=https://frontend.example.com
