# Copy to project root as .env and fill in real values.
# DSN format: user:password@tcp(host:port)/dbname?parseTime=true

PORT=8080
DB_DSN=user:password@tcp(localhost:3306)/dbname?parseTime=true

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
JWT_SECRET=minimal_32_character_secret_for_jwt_signing

# Optional: CORS for frontend on another origin
# ALLOW_ORIGIN=https://frontend.example.com
