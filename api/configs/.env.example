# Salin ke .env di root project dan isi nilai asli.
# Format DSN: user:password@tcp(host:port)/dbname?parseTime=true

PORT=8080
DB_DSN=user:password@tcp(localhost:3306)/dbname?parseTime=true

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
JWT_SECRET=minimal_32_character_secret_for_jwt_signing

# Opsional: CORS (frontend beda origin)
# ALLOW_ORIGIN=https://frontend.example.com
