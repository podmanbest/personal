{
  "name": "personal-portfolio",
  "private": true,
  "description": "Portfolio stack: API (Lumen), web visitor, admin",
  "scripts": {
    "install:all": "npm install && cd dashboard && npm install && cd ../web && npm install",
    "build": "npm run build:admin && npm run build:web",
    "build:admin": "cd dashboard && npm run build",
    "build:web": "cd web && npm run build",
    "dev:admin": "cd dashboard && npm run dev",
    "dev:web": "cd web && npm run dev",
    "preview:admin": "cd dashboard && npm run preview",
    "preview:web": "cd web && npm run preview"
  }
}
