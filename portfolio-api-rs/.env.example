# Port server (default 8000)
PORT=8000

# Log level (trace, debug, info, warn, error)
RUST_LOG=info,portfolio_api_rs=debug

# Database — sama seperti portfolio-api (Lumen): MySQL/MariaDB
# Opsi 1: DATABASE_URL (override penuh)
# DATABASE_URL=mysql://user:password@127.0.0.1:3306/personal_portfolio
# Opsi 2: variabel terpisah (default = personal_portfolio)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=personal_portfolio
DB_USERNAME=root
DB_PASSWORD=
